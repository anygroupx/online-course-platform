# 插件原生服务：实现与发布状态

> 更新：2026-09-10。这里的“集成”指将上游业务移植为 **Java / Vue 原生功能**，不是 PHP 安装器。
> **生产已于 2026-09-09 更新前后端代码。** 原生商城、服务订单、项目中心和资金功能已随代码发布但默认关闭；生产尚未运行迁移 018 / 019 / 020 / 021 / 022 / 023 / 024 / 025 / 026 / 027 / 028，也未向真实供应商下单或退款。后续独立发布已于2026-09-10更新镜像，并包含私有图文工单、本人用量及全局概览；相关功能仍受总开关和迁移状态约束，详见第7节。**经营者明细、结算流水、指定执行编号恢复、带初始额度开户及P05总公里服务为本轮新增、尚未发布**，不要套用此前部署结论。

## 1. 实现范围

12 个包对应不同业务与协议，不能用一个“已接入”状态代表全部功能完成。

| 包 / 类型 | 已移植到本平台的功能 | 尚未完成 / 限制 |
|---|---|---|
| P01 闪电 `flash` | sdxy / ydsjxy / xbd 账号预检、区域与计划选择、任务时间配置、报价下单、进度、暂停/恢复、延期、剩余次数退款、执行记录、任务改时、单任务延期、sdxy 短信授权及 sdxy/xbd 规则刷新 | 短信授权使用服务端短期会话；真实供应商仍待联调；列表有界扫描并精确匹配单号；未返回完成次数时明确显示未知 |
| P02 Benz | 课程/分类导入与增量进度、仅更新现有价格/说明；本轮新增指定执行编号的完整身份核对、五分钟预览和本地原子关联；查单/补单不再猜测第一条编号 | 价格更新代码已部署，依赖024且默认关闭；编号恢复依赖028、尚未提交/发布；自动找号和批量分页仍缺可靠语义，完整身份响应需授权联调 |
| P03 黑鲨 `heisha` | 商品 1/2/3/4 的账号预检、计划/区域与距离校验、报价下单、同步及上游退款核对；3/4 增加官方托管人脸采集、状态检查及单次授权下单 | 须显式批准官方 HTTPS 采集域名；平台不接收照片、不伪造令牌；真实采集与供应商仍待联调 |
| P04 极光 `jiguang` | 商品与学校、报价下单、状态、增次、退款预览/确认、执行记录 | 真实上游合同与资金流程尚未联调 |
| P10 无心 `wuxin` | 授权码查询、按周/时间段/配速下单、进度与日志、增次、退款、现有计划回填与编辑、重新分配 | 严格区分各动作成功码；退款响应缺少次数时转人工核对；没有服务端证据的“重跑/单次改时”不开放 |
| P06 实习 `sxdk_tw` | 九个平台的账号预检、学校选择、计划建议、按服务日报价下单、编辑/续期、暂停/恢复、立即执行、补交、取消退款、最近执行记录；另有平台 ShowDoc 本人验证通知 | 无实时价格接口，须管理员核实合同成本；真实微信授权仍缺；通知不是上游绑定，独立开关默认关闭；真实供应商未联调 |
| P07 `syyv5` | 上游项目发布、零余额开户、冻结费率充值/转回及本人工单；本轮补充带初始额度开户；另有本平台多客户子账、带初始额度开户、资金记录、独立主/客户密钥 OpenAPI 与下游本地售后 | 代码已部署但默认关闭、尚未真实联调；本地子账不是上游余额；本轮新增上下游私有PNG/JPEG图片、本人用量和双权限全局概览，代码已发布但仍关闭；下游图片依赖027，上游图片复用022；本轮补充经营者搜索/账户客户钻取及筛选结算流水，尚未发布；客户额度消费、安全业务入口、上游密钥生命周期、其他附件与未知新工单归属仍缺；不原样兼容PHP action接口 |
| P05 ssbenz `ssbenz_xbd` | 明文总公里计划的两种方案报价、原生下单、总公里/时段/星期快照、精确提交编号状态核对与未知结果保护 | 本轮新增、尚未发布；不推断方案0/1名称，不提供账号预检、完成进度、账号汇总归因或自动退款；加密核心仍未迁移 |
| P08/P11 鲸鱼、P09 appui、P12 雷电 | 明文外围功能和重复协议家族已识别 | 不透明核心需合法源码或正式协议；P08/P11 不重复注册同一家族 |

以上六类原生服务有本地协议与业务测试，**不等于获得真实供应商兼容性验收**。

## 2. 用户和管理员入口

- `/services`：按服务类型筛选商品；使用各插件专用表单，先读取可选计划，再预览服务器报价。
- `/service-orders`：所属用户的进度、执行记录、操作历史和可用售后动作。结果不确定时只检查状态，不自动重提。
- `/admin/service-products`：搜索、分页选择已验证接口，读取目录、设置零售价并上架；实习服务改为选择平台、填写有效期内的合同成本及核对依据，不请求不存在的目录/价格接口。商品与供应商的绑定不可更换；下架无需供应商在线。
- `/admin/service-orders`：核对不确定操作、查看上游单号和审计记录、为已核实的上游主动退款入账。
- `/admin/orders`：订单详情内的“恢复执行编号”；输入可信回执，先核对完整身份，再单独确认关联；本轮新增，尚未发布。
- `/admin/plugin-integrations`：保留为**管理员接口接入检查**，不是用户购买入口。此页查询目录本身不创建订单或扣款。

界面沿用现有主题变量和 Element Plus，支持移动端与深色模式。编辑无心计划回填当前配置，不用新订单的默认时间/星期覆盖旧计划。原始账号密码和授权码不存入浏览器持久化存储。

## 3. 后端架构

下图为付费服务下单流程；执行编号恢复等本地维护不进入扣款或派发链路。

```text
Vue 专用表单 → 服务端报价（READY，5 分钟有效）
                 ↓ 用户显式确认
本地事务：锁定操作 / 商品 / 订单，校验版本，记录订单与余额扣款
                 ↓ 事务提交后，仅一次上游请求
NativeServiceGatewayRouter → PhpNativeServiceGateway / WuxinNativeServiceGateway
                            / InternshipNativeServiceGateway / SsbenzDistanceGateway
                 ↓ ApiHttpClient → SafeHttpClient
本地事务：处理明确回执、更新订单、必要时记入退款账本
```

- `service_product`、`service_order`、`service_order_operation` 与既有 `account_ledger` 协作，不运行旧 PHP、SQL 或 cron。
- `PlatformDockingStrategyFactory` 仍只收录课程策略。服务提供商不能绑定课程平台，亦不能通过修改类型把既有课程配置原地转成服务配置。
- 保存配置 → 只读验证 → 显式启用；敏感配置变化使旧验证失效。订单保留供应商身份指纹，不能因换地址/账号而被重定向；既有五类服务的独立账号身份未变时可轮换密钥；P05令牌本身即账户身份，更换后旧订单不可自动迁移。
- 出站目标只来自已保存配置，保留 DNS/SSRF、TLS、重定向、超时和响应大小限制。关闭传输自动重试，不接受用户传入任意 `act` 或 URL。
- 实习使用已保存的完整直连 API 地址；GET 使用 `act/uid/key` 查询，POST 使用表单凭据，不追加清卡包装路径。习迅云学校路由只接受上游学校目录的 HTTPS 元数据，不接受客户端 URL，也不向用户返回该地址。
- 无心按原协议使用保存的 `u_uid/key` 查询凭据；学生授权码只在 POST 业务字段中发送。请求 URL 查询串、请求体和原始响应不写日志。
- DTO 只返回白名单字段、掩码账号与十进制金额字符串。敏感订单参数加密保存；已完成/过期参数清除，不确定操作的参数最长保留七天。报价与计划使用北京时间，不依赖宿主机时区。

## 4. 资金与售后约束

1. **预览不扣款**。Flash 报价最迟在最早任务时间失效；过期预览不当成已创建订单。确认时再次检查商品、供应商和订单版本；订单保留原零售单价与计费因子。报价过期或版本变化必须重新预览。
2. **不能重放写操作**。确认编号派发至多一次；HTTP 不自动重试。上游负码也可能包装超时，不能推断“肯定未受理”。
3. **结果不确定**：保存为 `UNKNOWN`，保留原扣款与待核对操作；进程中断的 `DISPATCHING` 超过五分钟也进入核对，不自动再次下单或退款。
4. **用户请求退款**：按明确回执的剩余次数、订单冻结单价结算，并以预览上限和已付净额封顶；余额、账本和订单状态同一事务更新。
5. **上游主动退款**：同步只标记 `REFUND_REVIEW`，不会凭状态字符串自动加余额。管理员查验上游单号/资金流水，填写实际次数及依据，先预览再确认本地入账；此流程没有上游写请求。Flash 未提供完成次数时不默认填入全额退款次数，须人工核实。
6. 人工核对和审计资料要求同时具备 `api-provider:update`、`payment:reconcile`。核对记录保存操作者与依据；订单所属用户看不到管理员证据和供应商凭据。
7. 单任务延期同样先预览再确认，仅使用当前订单已核实的任务 ID；不接受外来聚合订单号，不改变订单的暂停状态；结果不确定仍不自动重复提交。
8. 两个并发退款预览不能重复入账；账本写失败会回滚整个本地结算。结果响应丢失时只查询已保存编号，不补发写请求。

## 实习服务的计费与 UI

- 平台分别为职校家园、黔职通、工学云、校友帮、习迅云/宁夏、学习通、慧职教、广西职业、江西智慧教育；项目和商品 ID 必须一致。
- 不复制原插件硬编码价格。管理员核对合同服务日成本、倍率、取消条款并留下依据（最长 90 天）；售价不得低于成本，供应商身份变化或合同过期阻止新扣款，不阻断已付订单取消。
- 按北京时间今天至截止日（含首尾）和所选星期生成已购日期，一次最多 365 个日历日。节假日开关不减少已购天数；校友帮运行方式 3 的日价倍率为 5，创建后不能通过编辑换价。
- 续期/改星期只为**从未买过的日期**扣款；缩短不自动退款，恢复已经买过的日期不重复收费。立即执行单独按一次计费，不增加已购天数。新增费用四舍五入后不足一分时拒绝，而非免费追加。
- 取消采用公开的本平台未用服务日退款规则，须上游明确确认取消；按当前计划内尚未到期的已购日期、冻结日价计算，并以报价和净付款额封顶。这不代表上游返回了退款天数或承诺同额退款。
- 补交范围必须全部属于已购服务日且不晚于今天，不得跨过未购日期。界面禁选无效日期，服务端再次校验；黔职通无日报，工学云额外支持上班/下班/上下班打卡类型。
- 编辑回填当前计划，账号不可修改，空密码保留远端现有凭据。上游订单号、平台、日期/星期和运行方式必须与本地快照一致；跨零点报价重新预览。
- 订单展示已购天数与截止日，不伪造完成百分比；日志只展示最近十条的时间与安全类别，不返回可能含凭据的原文，也不把类别当作执行成功。
- 账号预检可返回独立 `advice`：上下班时间、截止日、星期与报告开关。用户点击“采用这些建议”才更改表单；未知字段不补默认值，不改变运行方式或已购日期，更换账号/学校/项目后丢弃旧建议。
- 本平台增加 ShowDoc 通知：保存本人密钥不会发送，显式验证通知、输入收到的六位验证码后才启用订单变化通知；非上游微信授权，不收取原包所谓“绑定费”。
- 通知凭据和待发送内容服务端加密；固定官方 HTTPS 目标，不接收 URL。验证码十分钟有效、最多五次尝试，重新配置后旧证明失效；订单转属后不能继承原渠道。
- 后台有界观察本地已同步状态，不额外查上游；通知只含平台订单编号、状态与计划日，不含账号密码、位置或报告。离线变化可能合并；周期结束不代表考勤成功，ShowDoc 受理不代表阅读。
- 验证与更新均有持久化发送记录；UNKNOWN 不自动重发，已收到的原验证码仍可验证。限流故障时拒绝发送；停止清除密钥并取消未派发消息，已派发内容不可撤回。通知不触发资金操作。
- 未开放：真正的校友帮微信授权及令牌采集、无可靠实现的评价/异步任务/地址搜索。原包“绑定”只有状态修改和收费，不复制为假绑定成功。

## Benz 仅更新现有课程价格与说明

- 管理员“课程平台管理 → 仅更新价格/说明”，独立于导入弹窗；需要 `platform:update`，Benz 原生类型为 `27`。
- 选择已启用接口、远程分类、排除分类与倍率，范围可为全部已关联课程或指定远程商品编号。只读获取上游目录，绝不为未导入商品创建课程。
- 服务端生成五分钟、最多500行的预览，展示本地名称、原价/新价及说明差异。十进制运算，最终四舍五入至两位；负数、溢出、正值不足一分、说明超过500字及重复上游编号均拒绝。
- 用户显式确认后只更新预览中的价格与已提供说明，不重新查询上游，不改名称、分类、排序、上下架、接口或密码规则；缺失说明保留原内容，明确空串可清空。
- 先锁定供应商与批次，再按本地ID锁定全部目标行；价格、说明或绑定被他人修改则整批 STALE，不覆盖、不部分成功。无关的名称/分类/状态修改会保留。
- 同批次并发或重复确认仅应用一次；响应丢失先查询原批次。若查询显示 READY，可重新显式确认**同一批次**，不自动提交或另建批次。APPLIED、STALE、EXPIRED 均不重放。关闭总开关后仍可读取结果。
- 依赖迁移024，默认关闭。该模式只操作本地课程价格，不向供应商写入、不影响原订单金额、用户余额或账本。

## Benz 指定执行编号恢复

- 真实订单详情新增“恢复执行编号”。管理员提供已核实的回执编号与依据、确认订单归属，服务端只读查单一次；完整身份一致后展示五分钟预览，再由用户单独确认关联。查询失败或身份资料不足不能认领编号；不是自动发现缺号，更不是补单。
- 需要有效账号同时具备 `order:update` 和 `api-provider:update`，服务层重查当前数据库权限。`user/pass/kcname/cid` 必须精确匹配，其中 `cid` 为商品绑定；学校/学生课程编号如有返回也不能矛盾。指定编号必须唯一，拒绝首行猜测、重复JSON键、拼接JSON及异常成功码。
- UUID先持久化READING，再于事务外调用一次 `chadan`；同UUID不重查，响应丢失仅读取原请求。手工依据加密，订单/商品/配置摘要使用服务端密钥保护，响应不包含凭据或原始查单内容。
- 确认重新校验权限、冻结身份和期限；数据库锁等待后再次检查到期。同源配置别名、归档历史及完整身份重复均参与归属检查，编号和订单均有唯一声明；声明、条件关联和审计一起提交或回滚。
- 仅更新订单的执行编号与更新时间，不改金额、状态、进度或凭据，不触发支付、退款、通知及供应商写请求。缺编号订单不能先查第一条后直接补单；已知编号查进度须精确匹配，下单消息里的数字不再被当作编号。
- 既有Benz查单/补单的安全修正不受原生服务总开关控制：缺少明确编号时将拒绝继续查询或补单。发布前应核查存量缺号订单并验证恢复契约；关闭新入口不等于保留旧的猜号行为。
- 支持操作者/订单范围的最近20次记录、原请求恢复、无自动POST重放、手机深色和旧响应隔离。接口见API文档第8.2节。依赖 `database/migrations/028_course_order_receipt_recovery.sql`；尚未提交/发布、生产未迁移、总开关仍关闭。仍需获授权验证完整身份字段，不能宣称所有Benz响应都已兼容或批量分页问题已解决。

## 闪电账号授权与规则刷新

- 新原生表单支持账号密码授权；闪动校园 `sdxy` 另支持短信验证码。账号授权会话与用户、商品、商品版本和供应商配置版本绑定，最多十分钟，只返回掩码账号和安全的计划/区域选项。
- 上游学生 ID、可复用密码只保存在服务端加密快照；验证码不落库，浏览器不接收上游令牌/密码，也不能在下单时覆盖会话账号。会话 ID 不是可跨用户使用的授权凭据。
- 发短信、验证码验证、规则刷新分别先持久化派发状态，再提交一次上游请求。网络结果不确定时只能检查同一会话，不自动重发。每个会话最多一次短信、一次验证、一次规则刷新。
- 复用安全限流：每账号至少 60 秒短信间隔、最多 5 条/小时，并限制用户/账号授权频率。手机号限流键使用带服务端密钥的摘要；限流禁用或允许故障放行时，此授权入口拒绝工作。
- `sdxy/xbd` 刷新规则只使用已核实且绑定该会话的学生 ID。上游确认刷新后重新读取规则；缺少可复用密码/学校信息时要求重新授权，绝不把原有选项假装成新规则。
- 报价与会话版本、到期时间绑定，刷新/撤销使旧报价失效。确认时在订单扣款事务内消费会话；同一授权创建的两个报价最多一个可用于下单，余额不足时会话消费一起回滚。
- 关闭表单或取消授权清空前端凭据并撤销会话；过期清除加密快照，失联请求进入 UNKNOWN。该模块已增加原生代码与回归测试，最终完整验证结果以本页开发验证段为准。

### 4.6 黑鲨官方托管人脸授权

- 商品 3/4 复用十分钟账号会话，密码预检成功后进入 FACE_REQUIRED，不能直接下单。采集入口需另行明确授权，只向已绑定手机号发送一次 `collect_link`；结果不明进入 UNKNOWN，不重放。
- 人脸业务令牌、预检令牌及完整采集地址仅保存于服务端加密快照。普通会话只返回掩码账号、计划选项、已批准域名和接收数量；不提供照片上传、照片展示或“手填令牌”接口。
- 配置 `HEISHA_FACE_ALLOWED_ORIGINS`，只接受明确批准的完整 HTTPS origin，不接受通配符、IP、用户信息或 URL 片段。未配置时不能上架人脸商品，也不会向用户标为可下单；撤回白名单后旧报价不能确认。
- 用户主动打开时，先取得本平台 60 秒一次性跳转凭据。浏览器将其通过表单 POST 至固定本地端点；服务器原子消费后才以 303 跳到官方地址。凭据不放查询字符串，不转交平台 JWT，窗口无 opener、跳转不带 Referer，也不由服务器代理采集网页。
- `collect_link` 回执即使称已完成也不能授权购买，必须另行 `face_check`。只接受明确布尔完成状态及一致的照片数量；检查不确定时允许手动重新读取状态，不重新采集或下单。
- 确认下单复用原子扣款/消费会话机制；账号、令牌、计划和商品不可跨用户替换。撤销、过期或配置变化阻止使用，迟到回执不能恢复授权。撤销仅影响本平台，不删除上游已经收到的资料。
- 此流程复用迁移 020，无新增生产迁移；本地及模拟工作流验证不能替代官方采集页面、令牌有效期和真实供应商兼容性验收。

### 4.7 Benz 课程同步对照

Benz 的目录/分类导入与订单进度属于既有课程模块，不注册为运动商品。批量进度现在拒绝错误状态、缺失数组、非法行及超大页；本地写入失败会回滚，不再吞错并推进水位。新水位固定在同步开始时，保留重叠窗口，并拒绝重复页、限制最多二十页，避免失控循环。供应商口径仍须联调，不能由原包不一致的分页注释推断接口语义。

本平台匹配已绑定的远端订单号，而不是按课程名猜测缺失单号。独立“仅更新现有价格/说明”模式已经实现，使用原子本地批次而不是普通重新导入；本轮已实现管理员指定执行编号的完整身份核对及本地原子关联；自动发现缺号与完整分页仍需可靠协议，不能按名称猜测归属。

### 4.8 syyv5 项目子钱包与上游工单

- 用户入口 `/service-projects`，管理入口 `/admin/service-projects`。沿用原生功能总开关，客户密钥仅服务端加密保管，不放入浏览器、iframe 或 URL。
- 接口地址是完整 HTTPS API 地址。连接测试仅读取目录；管理员须独立核实实际单位成本、售价、凭证和最长九十天有效期，不把目录价当成本、不把缺价默认为 1。每个账户冻结开户时的售价。
- 默认零余额开户；本轮新增可选初始额度，先展示冻结费率与费用，再独立确认开户和充值。充值与转回都先预览、再确认。预扣与操作保存在本地事务中，上游调用不持有 SQL 锁，回执另行结算。金额用 BigDecimal，充值向上取整到分、转回向下取整。
- 转回受本平台未退充值额度、付款预算和上游实际余额限制。UNKNOWN 保留原操作及必要预扣，不重发、不凭余额差退款；资金核对须接口管理与财务核对双权限。主密钥或地址变更会阻断旧账户继续使用，不当作安全轮换。相同规范化地址和主密钥的重复接口配置，仍受来源级客户/工单唯一绑定限制。
- 工单只显示本平台已绑定的本人记录，不代理上游主账号/项目宽范围列表。提交、回复及补偿审核均为本地草稿 → 明确确认 → 一次发送；内容加密保存，回显不执行 HTML，不加载外部附件。PNG/JPEG 图文扩展见4.12，代码已部署但仍关闭。
- 补偿审核只改变工单状态，**不自动向项目余额或平台余额入账**。上游 DTO 不含客户归属，因此未知的新工单禁止凭任意编号认领；既有工单的未知回复/审核须证据核对，不按相似文字自动判定成功。
- 移动端使用项目卡片、纵向操作记录和专用工单抽屉；结果丢失后的“检查”只查询原操作，不重放写请求。安全上游项目登录、真实上游密钥轮换/撤销、其他文件/外链附件仍未实现；经营者分页、账户/客户钻取与结算流水已完成本地实现，尚未发布；本平台项目 OpenAPI 与本地凭据生命周期见下节，二者不能混同。

### 4.8.1 开户时同时充值初始额度（本轮新增）

- 已有 `generateCustomer(project_id,balance)` 协议允许明确初始额度，本次补齐原先只开放零余额的缺项。真实项目中心默认不充值；可主动选择初始额度，先预览精确费用，再确认一次开户。不伪造客户购买/消费接口。
- 额度0.000001–100000、最多六位小数；冻结售价、有效合同成本与版本，金额向上取整到分。确认事务中原子预扣和持久化DISPATCHING，事务外一次创建调用；不先开户再自动追加充值，不因HTTP使用GET就重试。
- 回执必须完整且客户项目/初始余额一致，才能绑定并增加可转回额度和原实付预算。失联、异常回执或结算失败保留UNKNOWN和预扣，用户只能检查原操作；不能拿余额差猜测成功或自动退款。
- 双权限人工核对须有开户、初始充值及客户归属的完整依据。确认未受理后幂等返还原预扣；确认已受理则按冻结快照结算，不再扣款或发开户请求。管理员页面明确显示两种结果的资金影响。
- 成功的带初始额度PROVISION计入账户/经营者/全局已结算支出与资金流水，UNKNOWN和未受理不计。免费开户保留零额记录。复用021，无新增DDL；初始创建响应没有实际成本字段，真实成本仍须独立合同核实。
- 本地实现和模拟验收不代表真实项目服务可购买。该功能尚未提交/发布，生产开关仍关闭；真正客户消费、安全登录和供应商密钥生命周期仍未完成。

### 4.9 下游客户与本平台项目 OpenAPI

新增“下游客户与API”独立页面，不把每用户/项目唯一的上游钱包误作多客户池。一个本地项目可有多个本人客户，支持零/带初始额度开户、充值/转回、暂停/恢复/零余额关闭，以及本地资金与API调用记录。

- 下游客户创建、额度更新、平台钱包变动与账本在同一数据库事务结算，无供应商HTTP；页面明确区分“本地客户额度”与“已向上游充值/购买服务”。
- 客户采用创建时冻结的本平台发布单价。充值向上取整到分、部分转回向下取整，全部转回可退还残留实付分币，但从不超过客户实付预算与可退单位；不产生充值奖励或赠额套利。
- 五分钟预览，UUID请求编号与规范化参数绑定；同编号不同参数拒绝，同编号重复确认只结算一次。丢失响应先GET原请求/原操作，客户端不自动重提。客户/项目变更使旧预览失效。
- 本平台项目主API密钥有READ_ONLY/MANAGE两种权限；客户密钥默认 READ_ONLY，可查本人余额/工单；显式选择 SUPPORT 后可对本人提单/回复，不能审单、列其他客户或移动资金。随机256bit、哈希保存、当前密码验证、配置版本及1…365天有效期、仅签发时返回一次明文；可轮换/撤销，不涉及真实供应商密钥。
- 外部接口使用 `X-Project-Key` 请求头及 `/external/projects/v1`，不接受URL密钥，不把API凭据注入JWT或管理员权限。主密钥、客户密钥、既有课程APIKey三者分离。新原生契约不是旧PHP `action` 接口的原样兼容层。
- 额度消费与实际项目业务入口、经营者分页/钻取统计仍待完成；本人用量与全局概览见4.13；上游密钥生命周期、其他文件/外链附件等仍有缺口。客户子账依赖迁移025，本地售后依赖026；代码已部署，但生产迁移未应用且功能默认关闭。

### 4.10 下游客户本地售后

- “下游客户与API”的客户卡片提供“客户售后”，另有本人全部工单入口；支持建议、故障、补偿申请、图文/仅图片回复和经营者处理。客户可以通过显式授权的 SUPPORT 密钥在原生 OpenAPI 提单/回复。READ_ONLY 不新增写权限。
- 与上游工单、项目独立钱包完全分离。所有列表、详情、回复和请求回执校验 owner 与**确切 clientId**，不是只校验同一项目；即使项目相同，也不能查看或回复其他客户的工单。
- 每次写入绑定 UUID 请求编号、调用身份和规范化参数。工单、回复/审核及不可变请求回执同事务提交；同编号同参数只返回原回执，不重复操作。更新须匹配版本，避免旧回复覆盖新审核；暂停/关闭客户后，经营者仍可处理既有售后，客户凭据不能继续使用。
- 补偿金额仅作申请参考；审核通过/拒绝只写结论并结束工单，**不增加额度、实付预算或钱包余额，不创建账本流水**。已结束工单禁止追加回复或重复审核。下游本地图片支持见下；上游工单图片尚未接入。
- 响应丢失先查询原请求；查无回执才提供显式原编号重试/结束尝试入口，不自动重试、不自动升级版本。同页重开保留待核对请求的原客户；离开页面前应保存请求编号，可从界面按原编号查询。移动/深色布局保持纯文本呈现。

### 4.11 下游售后私有图片（代码已部署，默认关闭）

- 新建工单可附一张图片，回复可文字、图片或二者一起发送；原图限2MiB PNG/JPEG，服务端检验实际格式、单边4096/总400万像素，再仅复制像素重编码PNG，丢弃EXIF、注释和尾随数据。规范化结果最多4MiB；请求体（含chunked）最多3MiB，不接受外部URL或SVG/GIF。
- 图片加密保存于独立表，只在详情返回不透明imageId。鉴权通过后才读取加密内容，验哈希后以image/png、no-store、nosniff与限制CSP返回；图片ID本身不是授权，主/客户密钥同样校验owner与确切客户。
- 图片与工单/回复/原请求回执同事务，失败一起回滚；同编号换图会拒绝，旧纯文本请求保持幂等兼容。补偿仍不入账，图片不发给上游。
- 选图只在本地预览；已保存附件由用户点击后带JWT读取Blob，不把凭据放进img URL。关闭/收起/换图释放临时ObjectURL；换图和刷新内容后须重新勾选确认。无图片正文持久化到浏览器存储。

### 4.12 上游图文工单（代码已部署，默认关闭）

- 本人工单新增/回复支持内嵌 PNG/JPEG，回复可仅图片。图片选择与草稿预览不访问供应商；界面明确提示确认后发送给当前绑定上游，不与仅本地的下游客户售后混淆。
- 图片使用与下游相同的 2MiB 原图、4096 单边/400 万像素、去元数据重编码（PNG≤4MiB）策略；完整 JSON≤3MiB。规范化图片在确认时通过 `image_data` 转发，只有工单响应允许 8MiB 上限及总计 6MiB 规范化图片字符串，非工单资金/目录响应仍限256KiB。外链、SVG 等只显示不支持标记，绝不下载。
- 复用迁移022的加密MEDIUMTEXT草稿/快照，不增加上游图片表。详情仅返回附件标记；本人/真实接口管理员通过私有图片GET按需读取本地快照，no-store/nosniff/限制CSP，不触发上游刷新，浏览器不保存正文。
- 图文回显不匹配、缺失或本地结算失败保留 UNKNOWN，绝不重发。回复草稿冻结既有回复ID，确认/人工已受理核对要求相同文字/图片及新增ID，刷新不覆盖冻结证据；不能把历史同内容回复当成此次成功。已关闭/已解决工单不再回复或审核，未知新工单依然不能凭任意上游ID认领。
- UI共用私有图片选择/查看组件；换图或刷新清除旧同意，关闭抽屉/弹窗释放Blob。补偿审核仍不入账。真实供应商验收尚未进行，不承诺其他类型附件兼容。

### 4.13 本人 OpenAPI 用量与管理员概览（代码已部署，默认关闭）

- 本人用量从主密钥/登录身份派生，客户密钥不能读取经营者汇总。保留原始安全动作日志与资金记录入口，补齐累计/滚动24小时调用、动作分布、客户与本地工单计数；原“今日”含义改为明确起止时间，避免自然日误解。日志尽力保存，OK不是供应商下单成功，也不作为计费依据。
- 额度按项目分页展示活跃/暂停本地客户及实付可退预算；不同项目的单位不合计，不声称是上游余额。管理员概览须真实接口管理与财务核对双权限，同时在HTTP链与服务层校验。
- 全局人民币金额拆分为上游SUCCEEDED兑换与本地APPLIED子账；UNKNOWN/DISPATCHING只计待核对笔数，不混入结算金额。净扣款不是利润，两组流量不相加当收入。账户数仅反映本地绑定状态，不自动查询上游可用性。
- SQL只读可重复读、动作最多50类（标记其余分类）及项目分页上限50；报告限流、no-store、不返回正文/凭据，不写浏览器存储。失败不展示伪零，切换/关闭取消过期读取。复用021/025/026，无新增DDL或供应商请求。
- 用户“客户与项目 API → 用量与统计”、管理“运营统计”均为Vue原生页面，支持移动深色。经营者列表/钻取由4.14补充，尚未发布；真实客户额度消费仍缺失，不把概览当完整插件交付。

### 4.14 经营者明细与结算流水（本轮新增，尚未发布）

- 管理员在“经营者明细”按编号/用户名搜索，分页查看停用和已移除身份的历史资料，再进入项目账户、客户账户及资金流水。当前操作人仍须有效且同时具备接口管理与财务核对权限，不冒充目标经营者。客户按项目/状态筛选，账户余额未记录明确保留未知。
- 管理及本人流水支持项目、账本、资金方向、关键词和结算日期筛选；管理员还可筛选经营者，从客户明细进入时限定该客户。只统计SUCCEEDED项目开户/兑换与APPLIED客户额度，带初始额度开户计入实际费用、零额度开户保持零金额，不计UNKNOWN/DISPATCHING或失败；两类CNY金额分开，汇总覆盖全部筛选结果，不混合项目单位。
- 日期按北京时间，结束日期包含当天；同一UUID跨账本不去重，使用结算时间/账本/编号稳定排序。SQL先分页历史经营者再聚合资料，不在Java读取完整经营者集合；同一响应的计数/列表/汇总为只读可重复读快照，不承诺跨页快照。
- 精确字符串金额、余额空值、no-store、12次/分钟报告限流、安全投影、请求取消与失败不伪零均保留。编辑筛选后显式查询，翻页沿用已应用筛选；手机支持深色和长金额。接口契约见API文档第28节。
- 复用021/025/026，无新增DDL、资金写入或真实供应商请求。本轮未提交/部署；没有新增外部密钥的流水HTTP入口，不兼容旧PHP action格式，也没有把本地客户额度误当成真实服务购买。安全业务登录、真实客户消费及密钥生命周期等缺口仍存在。

### 4.15 P05 明文总公里计划（本轮新增，尚未发布）

- 独立服务类型 `ssbenz_xbd`，只支持 `project=xbd`、商品编号 `0/1`，不覆盖Benz课程策略或其他加密分支。编号尚无可验证的晨跑/日常跑语义，管理员须按已核实的说明设置商品名称。
- 配置为已授权的HTTPS API目录，例如 `https://service.example/xbd/ydapi`。API Key只映射到POST表单 `token`，不使用UID或用户提交的URL/密钥。固定请求 `/school`、`/add`、`/order`，不猜测PHP扩展名、不自动重试或切换路径；目录与连接检查不触发本应用的订单/资金写入。仅解析两项报价，忽略公告、响应SQL与未声明字段。没有独立UID证明，采用服务端密钥HMAC绑定规范化目录与令牌；不存可离线猜测的令牌哈希，更换令牌会阻止旧订单读取，不能猜测为同账户轮换。
- 数量固定1单，总公里数0.01–999999.99，最多两位小数（本地存储范围，不代表供应商业务上限）；其他次数服务仍保持0.1–50公里。开始/结束必须在06:00–22:59内且同日递增，星期1–7非空且不重复；学校选填，没有学校检索或账号预检。协议没有时区参数，业务验收须确认时段约定。
- Java按售价×总公里数精确计算最终金额，只在最后四舍五入到分；不足一分不生成可确认报价，避免经过六位中间值再次舍入。非凭据计划快照进入既有 `schedule_json`；账号密码仅在加密派发参数中，成功或核对关闭后清除。确认仍为事务内预扣/持久化、事务外一次HTTP、事务内结算。
- 创建的成功码及正整数编号仅证明提交记录，订单为 `SUBMITTED`，`completed=null`、`quantityUnit=单`。读取状态时只匹配已绑定编号；状态1仍只表示已提交，0/2进入 `SUBMISSION_REVIEW`，不自动认定失败、完成或应退款。响应列表上限1000项、256KiB；缺失、重复、矛盾或超限不更新订单，也不把缺失视为订单不存在。没有分页承诺，不使用存在缺陷的单号分支或账号汇总替代单笔证明。
- 不提供编辑、增次、日志或账号维度退单；总公里订单不能进入整数次数退款入账。负码、响应丢失、编号碰撞或本地结算失败保留UNKNOWN及原预扣，不重发；双权限、完整证据的“未受理”核对只能返还原扣款一次，“已受理”只绑定提交记录，不标记执行完成。
- 商城、确认弹窗、本人/管理订单与商品发布均为实际Vue功能；支持手机深色、44px触点、冻结快照和原操作恢复。复用018/019/020，无新增DDL；真实接口兼容性、价格和时间约定仍需授权验收。

## 5. 启用前的部署步骤

**须先取得新资金功能及数据库迁移的明确部署授权；本节不是已执行记录。**

1. 在隔离环境备份数据库，核对 `api_provider.id` 的实际类型与外键条件，审阅依次执行 `database/migrations/018_native_service_orders.sql` 、`database/migrations/019_internship_service_plans.sql` 、`database/migrations/020_service_account_sessions.sql`、`database/migrations/021_native_project_wallets.sql` 、`database/migrations/022_native_project_tickets.sql` 、`database/migrations/023_native_service_notifications.sql` 、`database/migrations/024_existing_course_price_refresh.sql`、`database/migrations/025_native_project_clients.sql`、`database/migrations/026_native_project_client_tickets.sql`、`database/migrations/027_native_project_client_ticket_images.sql` 与 `database/migrations/028_course_order_receipt_recovery.sql`。仓库基线为有符号 BIGINT；H2 回归不能替代生产 MySQL DDL 验证。
2. 配置强加密密钥和既有安全出站策略；不要复制原 PHP 包内的凭据或地址。人脸商品另须通过可信渠道核实官方采集站点，并配置 `HEISHA_FACE_ALLOWED_ORIGINS`（逗号分隔的完整 HTTPS origin，默认空）。
3. 从生产现有配置注入 `VITE_TURNSTILE_SITE_KEY` 后构建前端。普通开发构建不能直接替代生产验证码配置。
4. 首次发布保持 `NATIVE_SERVICES_ENABLED=false`；迁移与授权完成后才设置为 `true`，由 Compose 映射至 `APP_NATIVE_SERVICES_ENABLED`。通知另由 `NATIVE_SERVICE_NOTIFICATIONS_ENABLED=false` 控制，须审阅 023、取得接收渠道授权并验证后独立开放；对应 `APP_NATIVE_SERVICES_NOTIFICATIONS_ENABLED`。
5. 使用管理员自有、经授权的供应商测试环境保存独立 `flash/heisha/jiguang/wuxin/sxdk_tw/ssbenz_xbd` 配置，按各服务支持范围逐动作联调成功码、价格单位、订单号、退款及账务；P05另须核实两方案含义、总公里上限和时段约定，不用真实用户订单试错。
6. 测试通过后显式启用供应商、上架商品；先小范围验证购买及售后，再放量。不要把余额退款当成测试数据清理手段。

关闭功能标志会关闭原生服务入口及本地维护任务，不删除订单或账本。事故时先停止新业务、保留数据库和日志，再评估回滚；已发生的资金记录不能随应用版本回滚删除。

## 6. 开发验证

2026-09-10 P05完整本地检查点：**1303项后端测试通过**（course-web 1296项、course-common 7项，失败、错误、跳过均为0）；前端**99项单元测试、20组浏览器工作流及构建通过**，研究工具10项回归通过。后端最终完整构建于13:58:16（UTC+8）成功；758份源码、配置、数据结构及仓库规范摘要与最终构建前一致。以上均为本地验证，不代表生产已经开放。

P05新增103项后端回归（85项协议、15项真实H2/MyBatis账本事务与业务边界、1项目录集成、2项HTTP边界），8项前端单元测试及第20组实际Vue浏览器流程。覆盖最终金额精确舍入、公里/时段/星期、令牌身份变化、单次派发、未知结果保留预扣、严格编号匹配、提交状态不冒充完成进度、人工核对不接收退款次数以及手机深色。隔离MySQL8.0.44通过28项存储断言，使用真实018/019/020验证公里范围、金额、快照、唯一编号和状态；容器无网络、无生产挂载且已移除。该MySQL检查不包含account_ledger，账本事务与并发由真实H2/MyBatis回归覆盖。最终重新导出的160份经营者/流水SQL、13份报告SQL及22份编号恢复SQL均与此前隔离MySQL验证版本一致。

首次P05全量运行发现新增HTTP测试误把既有422错误契约写为400，按仓库契约修正断言后通过；随后统一错误文案使两个既有文案及UTF-8报文长度断言需要同步。未修改错误分类、HTTP行为或放宽响应大小限制，最终重新执行完整clean verify通过。

```bash
mvn -B -f backend/pom.xml -pl course-web -am clean verify
npm --prefix frontend test
npm --prefix frontend run test:browser
npm --prefix frontend run build
```

测试覆盖：六类协议白名单/成功码/字段，真实 H2 事务与并发确认，资金回滚、人工核对权限、订单归属、供应商身份变化、失效报价、UTC 宿主机、原生商城和管理员售后交互。浏览器使用真实 Vue 组件和模拟 API，不接真实供应商。

2026-09-10 P05变更前的完整检查点：**1200 项后端测试通过**（course-web 1193 项、course-common 7 项）；前端 **91 项单元测试、19 组浏览器工作流及构建通过**，研究工具10项回归通过。实习通知/建议、订单转属、未知结果恢复、旧确认失效，以及 Benz 独立价格更新的精确小数、原子回滚/并发与字段保留均验证。迁移 023 / 024 分别在无网络临时 MySQL 8.0.44 验证约束、事务回滚、过期凭据清除及大快照存储，未使用生产挂载。

带初始额度开户新增27项后端回归（18项真实H2资金事务/并发、6项协议、1项资金汇总、2项HTTP边界），3项前端单元测试及第19组浏览器流程。验证免费默认、非零精确费用、单次预扣与创建、资金不足回滚、初始回执不匹配、响应丢失、双权限核对、转回预算及用户/管理员手机界面。隔离MySQL8.0.44通过116项断言（保留97项经营者/流水断言，再加19项带初始额度开户核对）；最新编译导出的160份记录SQL及13份报告SQL与验证版本一致，报告仅1份受本次修改影响、另外12份与前一检查点相同。该P07检查点全量测试前后722份源码/配置摘要一致，暂存区为空；没有提交、部署、生产迁移或真实供应商/资金操作。

P02编号恢复新增50项协议/真实H2事务回归及2项HTTP权限回归，覆盖完整身份、结构化编号、拼接JSON拒绝、同源别名、撤权、独立确认、锁等待期间过期、并发唯一声明和原子回滚。新增前端5项单元测试及真实订单详情浏览器流程，覆盖响应丢失后仅读原请求、最近记录、错误/残缺结果、切换隔离与手机深色。隔离MySQL8.0.44完成028迁移与28项断言；最新编译重新导出的22份MyBatis绑定SQL与已验证版本一致。P02检查点的720份参与构建/测试的源码与配置摘要一致，没有提交、部署、生产迁移、真实查单或资金操作。

经营者/流水新增18项真实H2/MyBatis测试与3项完整HTTP过滤链路测试，覆盖停用/缺失身份、转义搜索、分账本分页汇总、结算日期、权限与无资金副作用。浏览器覆盖真实管理/本人入口、账户客户钻取、精确长金额、已应用筛选、错误/残缺响应、请求取消及手机深色。隔离MySQL8.0.44使用实际MyBatis绑定SQL完成97项断言，含16种流水筛选、并发提交下的只读快照；容器无网络/生产挂载，已清理。本轮功能未提交或部署。

此前P07检查点的首次全量后端运行在高负载期间出现4项既有计时断言失败（临时TLS证书生成及三个数值指数保护用例）；原断言和代码均未放宽，33项定向复测及随后完整1121项重跑通过。构建仍有既有Sass弃用与Vite大包提示；普通开发构建不等于已注入生产验证码配置。

图片新增验证：真实格式/伪造类型、像素与请求体上限、元数据清除、并发限额、私有图片鉴权、图文原子回滚、仅图片回复与临时Blob清理；上游图文回显、旧回复ID排除及UNKNOWN不重发均覆盖。本人/主密钥隔离、双权限全局概览、滚动24小时边界、分项目额度和精确CNY统计均验证。一次旧闪电授权浏览器流程出现等待超时，保持原断言重跑后通过，图片流程无该超时。

上游图文工单复用022，7.5MB加密草稿/8.5MB快照及回滚另在隔离MySQL验证。统计查询使用实际MyBatis绑定SQL，在相同版本的隔离MySQL验证owner/global范围、窗口边界、小数精度和并发提交下的只读可重复读；临时容器已清理。

迁移027另在断网、无生产挂载的临时MySQL8.0.44验证大加密字段、外键插入顺序、1062/1452/3819约束以及图片/回复回滚；临时容器已清理，未执行生产迁移。

迁移 018-026 已在临时 MySQL **8.0.44** 数据库按顺序完成 DDL 冒烟验证，临时数据库已清理；该验证不等于授权或执行生产迁移。事务、并发、来源绑定与幂等边界另由后端测试覆盖。

新增项目账户、工单归属、补偿审核不入账、未知响应/本地结算失败、双权限 HTTP 路由、数值精度及异常指数边界测试。重复配置相同地址和主密钥也不能把同一上游客户或工单绑定给两个用户。浏览器回归修正了多条提示同时出现时的单元素等待问题，业务断言未减少。真实供应商仍未验收，模拟截图不能当作真实订单记录。

## 7. 生产发布边界（2026-09-09）

### 2026-09-09 基线发布

2026-09-09 已从当前 `dev` 代码重建前后端 `latest` 镜像；本次发布边界如下：

- 管理员只读“接口接入检查”继续可用；匿名受保护 API 保持 401。
- 生产未执行迁移 018-026，`NATIVE_SERVICES_ENABLED` 与通知开关保持关闭；新原生页面和接口代码存在，但业务调用会被功能开关拒绝。
- 未发起真实供应商下单、退款、短信、人脸采集或 ShowDoc 推送。
- 2026-09-06 镜像 `course-platform-backend:rollback-20260906-205408`、`course-platform-frontend:rollback-20260906-205408` 保留为历史回滚点。

本次只重建并更新前后端，不执行数据库迁移或重建持久化依赖服务。前后端健康检查通过后才视为发布完成；启用原生功能仍须另行获得迁移和真实供应商联调授权。


### 2026-09-10 独立后续发布的只读核查

另一任务提交 `169ee86`（工单图片、统计与用户文案）及 `590f3ac`（测试稳定性），并更新了运行镜像。本轮实现/回归任务没有执行部署或生产迁移；仅只读核查当前运行状态：

- 后端：`sha256:34dce2c0c24e21ce057e4c1bcc377225e58c6ab8454b4738dbb0e66834aaed07`，健康。
- 前端：`sha256:6dba63184d5a5d0be20c26569dd81d5177b7a5df99a4da00623a711ec1a2eb66`，健康，包含新增统计页面资源。
- 工单图片与统计的新增类已在运行镜像确认存在；不声称不同构建环境生成的二进制逐字节相同，也不把类存在当作真实供应商验收。
- `APP_NATIVE_SERVICES_ENABLED=false`、`APP_NATIVE_SERVICES_NOTIFICATIONS_ENABLED=false`。代码已发布不等于业务已开放；本轮没有访问生产数据库、执行迁移或触发真实供应商/通知。
- 浏览器回归保留独立文案修改；测试服务改用独立随机端口及临时依赖缓存，解决并行任务的5173冲突。通知测试等待真实输入可用后点击可见标签，保留授权、单次发送与未知结果断言。
